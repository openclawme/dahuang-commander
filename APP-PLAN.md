# 大荒 · 主人 App 开发方案

> v1.1 · 2026-09-17
> **范围已收缩：一期只做安卓，鸿蒙推迟到二期**（主人 2026-09-17 决定）。
> 本文中鸿蒙相关章节（§3 选型对比中的鸿蒙列、§5.2 推送、§7.1 / §7.3 / §7.4 装机与工具链、§10 部分风险）**保留作为二期参考资料，一期不执行**。

---

## 零、结论先行（一期：安卓）

**一期只做安卓，鸿蒙推迟——这个收缩砍掉了绝大部分复杂度：**

| 出局的东西 | 原因 |
|---|---|
| 华为开发者账号 + 实名认证 | 不需要 |
| AppTest 工单 / 设备 UDID 注册 | 不需要 |
| 调试证书 1 年有效期 / 开发者模式 / 纯净模式 | 不需要 |
| HarmonyOS CLT / DevEco Studio / ArkTS / hvigorw | 不需要 |
| 新加坡那台 Linux 上的鸿蒙 SDK（约 10G 磁盘） | 不需要 |
| **深圳那台 Windows 云主机** | **可以完全不动** |

安卓侧的前置条件几乎为零：**不需要任何账号、不需要注册、不需要审核**。自签名 APK 发到手机上点一下就装好（顶多关一次纯净模式）。

**必须认下来的代价**：您自己的手机是纯血鸿蒙，**装不了 APK**——一期做出来的东西，您自己用不上，只能给有安卓机的朋友用。（除非卓易通那条安卓兼容层的路成立，正在核实。）

**技术选型尚未定**（见 §3）：三条候选路各有未知，正在核实——
- **原生 Kotlin + Compose**：无未知，工作量最大
- **Tauri 2 移动端**：可复用现有桌面端 React 代码（约 4400 行），但 Linux 上出安卓包待验证
- **小程序容器**：**可能让现有 2.65 万行小程序代码几乎零改动跑进安卓 App**，若成立则最省

**一条已经确定的原则**（主人提出，我认同并已写入 §3.3）：**共享契约，不共享 UI**。两个系统的特性会越走越远，跨端统一层的收益随时间递减（实锤是**版本耦合**，不是特性差异），但 API 契约、数据模型、设计 token 值得跨端共享。

---

## 一、已核实的前提（事实清单）

| 事项 | 结论 | 依据 |
|---|---|---|
| 后端位置 | **阿里云新加坡**（8.219.151.58），Next.js + PostgreSQL + nginx + socket 全在境外 | 本机 ip-api / DNS 核查 |
| 域名备案 | `dahuang.land` **无任何 ICP 备案记录**；`.land` 后缀不可备案 | 工信部备案查询 |
| 服务器备案 | 境外服务器**无需备案**，也不受接入商拦截 | ICP 备案规则 |
| 小程序状态 | 仅开发版自用，从未公开发布 | 主人确认 |
| 主体 | 仅个人身份 | 主人确认 |
| 目标鸿蒙机 | **纯血鸿蒙 5/6/7**（NEXT），装不了 APK，必须有 HAP | 主人确认 |
| 分发范围 | 先自用，验证后再说 | 主人确认 |
| 登录端点 | **`POST /api/agent/auth/login` 已具备**：账号（名字或 DID）+ 密码 → **10 年 JWT**，带失败锁定与防枚举 | 后端源码 |
| App 协议 | `Authorization: Bearer <JWT>` + `X-Agent-Version: 7.0`，**客户端中立** | 后端源码 |
| 本机 GLIBC | 2.39 ✓（鸿蒙要求 ≥ 2.28） | 本机核查 |
| 本机 JDK | **无**（鸿蒙要 JDK 17，安卓也要） | 本机核查 |
| 本机 Android SDK | **无**（`ANDROID_HOME` 空） | 本机核查 |
| 本机磁盘 | **仅剩 30G**（79G 已用 46G）⚠️ | 本机核查 |
| 本机内存/CPU | 14G / 4 核（鸿蒙官方推荐 16G，最低 8G）✓ | 本机核查 |

**合规灰区（如实标注）**：工信部信管〔2023〕105 号要求「在境内从事互联网信息服务的 APP 主办者应当履行备案手续」，禁止接入商/分发平台/终端厂商为未备案 APP 提供接入分发。**「境内个人 + 境外服务器 + 自行分发」这个组合，官方文件里没有对应条款，未找到可靠来源给出明确结论。** 自用（不公开分发）风险极低，但请知悉这是灰区而非白纸黑字。

---

## 二、两条路线（已选 A）

| | **A. 自用侧载**（已选） | B. 公开上架 |
|---|---|---|
| 备案 | 不需要 | 必须（且要换可备案域名 + 境内服务器） |
| 主体 | 个人可 | 生成式 AI 备案/大模型登记基本只受理企业类主体 |
| 周期 | 可立即开工 | 3–6 个月，资质为主线 |
| 装机 | 手动传包 | 商店下载 |
| 风险 | 低 | AI 类目可能被卡死 |

---

## 三、技术选型

### 3.1 硬约束

1. 必须产出 **安卓 APK** 与 **鸿蒙 HAP** 两种包；
2. 构建环境**只有这台 Linux 服务器**（除非您另有 Windows/Mac）；
3. 鸿蒙侧跨端框架全部要 DevEco GUI → **在纯 Linux 上，鸿蒙只能写原生 ArkTS**。

### 3.2 候选对比

| 方案 | 安卓 | 鸿蒙 | 纯 Linux 可行 | 复用现有代码 |
|---|---|---|---|---|
| **原生双端**（Kotlin+Compose / ArkTS+ArkUI） | ✓ | ✓ | **✓ 两端都无未知** | 协议层可照搬 |
| uni-app x（一套 Vue3+UTS） | ✓ | ✓ | ✗ 鸿蒙需 DevEco GUI | 小程序 WXML 思路最接近 |
| Tauri 2 移动端 | ✓ | ✗ | ✓ | 桌面端 React 可直接复用 |
| Flutter | ✓ | △ Flutter-OH 未验证 | 待实测 | ✗ |
| RNOH | ✓ | ✓ | ✗ 需 DevEco GUI | 桌面端 React 部分可复用 |
| 移动 Web / PWA | ✓ | ✓（浏览器） | ✓ | 改动最小 |

### 3.3 推荐：原生双端（共享契约，不共享 UI）

**理由**：在「纯 Linux + 必须双端」这个约束下，它是**唯一两端都没有未知**的组合。更关键的是——**Compose 与 ArkUI 是同一套心智模型**（声明式 UI + 状态驱动 + 组件树），`@Composable` 与 `@Component` 概念几乎一一对应。因此"写两份"的实际成本远低于 2 倍，API 客户端甚至可以直接对译。

小程序那 2.65 万行 WXML 不做 1:1 移植——两端都是**新写移动优先的 UI**，只复用后端协议与交互设计。

**为什么不用跨端框架把两端「统一」掉？**（主人提出，我认同）

两个系统的特性会越走越远，统一层最终会退化成「每个平台各写一遍 + 再叠一层框架的坑」。但比「特性差异」更锋利的证据是**版本耦合**——跨端层永远滞后于平台，而这个等待不掌握在我们手里：

- HBuilderX 4.61+ 要求 DevEco Studio ≥ 5.0.7.210；4.81+ 要求 ≥ 5.1.0.849；**HBuilderX < 5.14 必须配 DevEco ≤ 6.1.0，否则热更新失效**；
- 鸿蒙 API 26 于 2026-08-29 发布，且从 26.0.0 起才改用语义化版本 X.Y.Z——平台本身在快速演进；
- **推送是最典型的反例**：鸿蒙走 Push Kit，安卓要对接华为/荣耀/小米/vivo/OPPO **五家各自的通道**——这一层**任何跨端框架都统一不了**，两端本来就要各写一遍。

**但要分清两件事：**

| | 统一什么 | 判断 |
|---|---|---|
| **框架级统一** | 统一 UI 与平台能力 | **不做**——风险大，收益随时间递减 |
| **契约级统一** | 统一 API 契约、数据模型、设计 token | **要做**——价值大，风险为零 |

具体做法：API 契约写成一份文档 + 类型定义（端点、字段、错误码），两端客户端各约 300–500 行，照着同一份契约实现；设计 token（颜色/间距/字阶）做成一份 JSON，两端各自映射。**唯一值得跨端共享的是契约，不是界面。**

> **uni-app x 什么时候仍然值得选**：如果您更看重「一期最快出成果」而非「长期不欠债」，且能接受把两个平台的适配押在 DCloud 一家身上。它省力是真的（一套代码双端），风险也是真的（版本耦合 + 单点依赖）。**对本项目我倾向不选**——现有小程序有 23 个页面，这个 App 会持续长大，而框架的天花板会成为 App 的天花板。

### 3.4 已确认的鸿蒙 CLI 工具链

```
JDK 17 + Node ≥ 22（本机 Node v22.22.0 ✓）
  └─ HarmonyOS Command Line Tools Linux x64 ≥ 26.0.0（官方下载中心，需登录华为账号）
       └─ 自带 HarmonyOS SDK：hdc / hvigorw / ohpm / hap-sign-tool.jar
            └─ npm i -g @deveco/deveco-cli，export DEVECO_CLI_CLT_PATH=/opt/command-line-tools
                 ├─ devecocli create          建工程
                 ├─ devecocli auth login      浏览器授权
                 ├─ devecocli signature generate  自动配置调试签名（需插着真机）
                 ├─ devecocli build --build-mode release
                 └─ devecocli run             构建→装机→启动（支持热重载）
```

**备选（全手工，不依赖 devecocli）**：`hvigorw assembleHap` → `hap-sign-tool.jar sign-app -signCode 1` → `hdc install`。

> ⚠️ **两个必须避开的坑**：
> 1. **别用 repo.huaweicloud.com 上的 OpenHarmony SDK Linux 版**——那是给开发板/开源设备的，跟商业鸿蒙手机不是一套体系，装不上。正确的 SDK 在 CLT 里自带。
> 2. 从 5.0.2 Release 起**必须开启代码签名**（`-signCode 1`），否则构建出的包装不上设备。

---

## 四、一期功能范围

小程序现有 **23 个页面**，其中 index(2933)、dahuang(2109)、settings(1500) 三个就占了半壁。**一期不移植全部，只做主人每天真正会用的 5 条流程：**

| # | 流程 | 对应小程序页 | 说明 |
|---|---|---|---|
| 1 | **登录** | — | 账号 + 密码 → JWT，存 Android Keystore / 鸿蒙加密存储 |
| 2 | **总览** | index | 分身状态、Karma、待办摘要 |
| 3 | **对谈** | chat + room | 私聊 + 群聊，socket 实时（核心中的核心） |
| 4 | **任务与决策** | tasks + task-detail + decisions | 查看与审批 |
| 5 | **通知** | notifications | 列表 + 已读 |
| 6 | **设置（瘦身版）** | settings | 账号、服务器地址、版本、退出登录 |

**二期候选**（不在本期）：购物线（market / goods-detail / orders）、知识库、记忆、MCP 管理、联系人、大荒论坛（dahuang）、日程。

### 4.1 无 adb 环境下的调试通道（一期必须建）

本方案有个特殊之处：**构建在新加坡服务器上，手机在您身边，中间没有 adb**。安卓不需要像鸿蒙那样注册设备，但没有 adb 就没有 logcat——**出问题只能靠猜**，这对一个要反复迭代 UI 的项目是致命的。

**解法：让 App 自己把日志送到后端，我从服务器上读。**

- 新增一个极简端点（如 `POST /api/app/log`）：App 收集运行日志与未捕获异常，批量上报；服务器追加写入日志文件（带轮转，避免撑爆磁盘）
- 崩溃在下次启动时补报（本地先落盘）
- 开关放设置页：默认只报错误，排查问题时可临时打开全量日志

后端目前**没有**这个端点（`/api/agent/report` 是内容举报，不是日志），需要新写。**这是为"远程开发"付的一笔小税，但没有 adb 的环境下它是必需的**——否则我看不见崩溃，只能靠您截图描述。

> 若您手边有一台能插 USB 开 adb 的电脑，这一条可降级为可选。

---

## 五、账号、登录与推送

### 5.1 登录（无需任何第三方）

后端**已经**有 `POST /api/agent/auth/login`——账号（名字或 DID）+ 密码 → 10 年 JWT，自带失败锁定（429 / 15 分钟）与 dummy bcrypt 防枚举。**后端零改动。**

个人主体用不了微信登录（微信开放平台移动应用不接受个人），但**这里根本不需要它**。手机号验证码也不必（阿里云号码认证虽支持个人，但一期无必要）。

### 5.2 推送（一期不做真推送，这是有意为之）

| 通道 | 未上架能否用 | 结论 |
|---|---|---|
| 鸿蒙 Push Kit | **能用**（官方 FAQ：开通无特殊限制，不要求已上架） | 但 `testMessage` 每日 1000 条上限；未申请自分类权益时按资讯营销类算，**单设备每日仅 2 条**；服务与通讯类权益未上架申请受限 → **二期再做** |
| 安卓 · 华为/荣耀 | 不要求上架 ✓ | 可用 |
| 安卓 · 小米/vivo/OPPO | **要求已上架** ✗ | 自用路线用不了；聚合 SDK（友盟/极光）只是转发层，继承同样限制 |
| WebSocket 常驻保活 | — | **不可行**：鸿蒙 NEXT 无长时任务切后台约 **16 秒**断连；有长时任务约 1 分 15 秒 |

**一期务实方案**：**前台 socket 长连保持实时 + 冷启动/回前台拉取** `GET /api/agent/notifications`。自用场景下这条路够用，且零后端改动。

> ⚠️ **待实测风险**：鸿蒙侧需要 socket.io 客户端，ohpm 上是否有可用包**未验证**。兜底方案是退化到 HTTP 轮询（后端已有 `/api/matrix/client/v3/sync?since=` 增量接口），不影响一期交付。

---

## 六、后端复用边界

**结论：一期几乎不用改后端。**

| 需求 | 现状 | 要做什么 |
|---|---|---|
| 登录 | `/api/agent/auth/login` 已具备 | 无 |
| 协议 | Bearer + `X-Agent-Version: 7.0`，客户端中立 | 无 |
| 通知 | `/api/agent/notifications` 已存在 | 无 |
| CORS | 原生 App 的 HTTP 请求**不受同源策略约束** | 无 |
| 域名/证书 | `https://dahuang.land` 境外 + HTTPS 有效 | 无 |
| 设备注册表（推送用） | 不存在 | **二期**（届时新增 Push token 表） |

---

## 七、分发、装机与构建环境

### 7.1 鸿蒙装机：官方只有四条路，其中只有一条不需要电脑

华为官方 FAQ 把「把测试包装到测试机」穷举为恰好四条路径：

| # | 方式 | 需要电脑？ |
|---|---|---|
| 1 | `hdc install`（USB 连电脑） | 需要 |
| 2 | DevEco Studio 选真机 Run / Debug | 需要 |
| 3 | DevEco Testing 工具（USB 投屏安装） | 需要 |
| 4 | **AGC 邀请测试 / 公开测试** | **不需要** ✓ |

> 手机浏览器下载 HAP 直接安装、"应用市场-安装包管理"装未上架应用——**都未找到可靠来源支持**，且官方穷举清单里没有这两条。**别指望扫码直装。**

**主推：AGC AppTest 邀请测试（免电脑）**

- 官方明文：**「申请邀请测试，应用需要先备案吗？不需要。」** ← 这对自用路线是关键松绑
- 流程：AGC 上传测试版本 → 生成**公开链接**（无需邀请码，任何人都可分享）→ 手机**系统浏览器**打开 → 安装 AppTest 客户端 → 安装测试应用。**全程零电脑。**
- ⚠️ **受限开放**：官方原文「我们正在逐步为各个应用开放 AppTest 的邀请测试能力……可以通过在线工单系统与我们联系」，工单分类选「上架与运营-应用市场-应用测试」，需提供开发者名称、开发者 ID、应用名称、APP ID。官方文档整体偏向企业开发者，**个人账号能否开通未明确** → **这是最该先验证的一件事**（见 §8 M0）。
- 限制：**安装后 90 天**到期无法启动；测试版本可能需轻量审核。

**保底：hdc USB 装机**（需要手机旁边有一台电脑）——只需要 `hdc` 一个小工具，不需要整台开发机。

### 7.2 安卓 APK
1. `keytool` 生成 release keystore（**加密备份，绝不入库**）；
2. Gradle CLI 出签名 APK；
3. 放到本机服务器或网盘 → 生成**二维码**；
4. ⚠️ **微信内打开下载链接会被风控**（未备案域名 + APK 下载类是双重触发条件）→ **只发二维码，让对方用系统浏览器扫**；
5. ⚠️ 微信传 APK 会被自动改名成 `.apk.1` → 打包成 `.zip` 发，或直接用二维码；
6. 对方需给"实际发起安装的那个 App"单独授权"安装未知应用"（Android 8.0+ 没有全局开关）；华为/荣耀还需在弹窗里临时放行或关"纯净模式-增强防护"。

> 装机指引要写成**一页纸图文**（各品牌路径不同），这属于交付物的一部分。

### 7.3 签名：26.0.0 起不再需要本地插着手机

官方原文：**「从 26.0.0 版本开始，支持在 AGC 注册设备后开始签名。」**（`ide-signing-auto`）

流程：手机取 UDID（`hdc shell bm get --udid`）→ AGC 注册设备 → **云端完成签名**。这条对「云上构建」是关键的解放——以前自动签名要求本地连着设备。

- 同一设备重装/更新同一应用：**不需要**重新注册 UDID、**不需要**重签；
- **新增设备才需要**重出 Profile 并重签（一个应用最多 100 个 Profile，单 Profile 最多 100 台设备，账号最多管理 100 台设备）。

**保底装机步骤**（USB，走 §7.1 第 1 条）：
1. 注册**华为开发者账号并实名**（个人即可，免费）——**这一步只能您本人做**（需身份证/人脸）；
2. 手机开开发者模式（连点 7 次软件版本）+ 开 USB 调试；
3. 注册 UDID → 申请调试证书 + 调试 Profile（Profile 里必须勾选目标设备）；
4. `hdc install` 装机。被纯净模式拦时**关「增强防护」**。

> ⚠️ **两个现实代价**：
> - **调试证书有效期 1 年**（未实名只有 14 天），到期要重签重装；
> - **调试签名的应用只能在开发者模式下运行**——给朋友装，等于**朋友的手机要长期开着开发者模式**。
>
> 这也正是 §7.1 的 AppTest 更值得争取的原因：它既不用插电脑，也不用开发者模式。

**明确走不通的**（已逐条查证）：发布证书自签 + `hdc install`（设计上不允许）、「指定设备发布」（**仅企业开发者**）、蒲公英 HAP 分发（要求发布证书 + 指定设备发布 Profile → 个人同样受阻）、企业签名（需企业主体）。

### 7.4 构建环境：三台机器怎么分工

| 机器 | 能力 | 定位 |
|---|---|---|
| **新加坡 Linux**（本机） | 后端在跑；CLT + hvigorw 可命令行构建 | **主力：后端 + 构建 + 我全程驱动** |
| **深圳 Windows**（4C/16G/80G/200M） | Windows 版 CLT（官方支持，命令与 Linux **完全一致**）；`deveco-cli` 走 **Auto_Detect**（比 Linux 省事）；HBuilderX `cli.exe` | **备选构建机 / GUI 窗口** |
| **您的本地电脑** | 预览器 + USB 真机调试 + 模拟器 | **开发迭代只能在这里** |

**⚠️ 云主机当不了开发机——三重阻断，全是官方明文：**

1. **模拟器**：华为官方原文**「不支持在虚拟机系统中运行模拟器」**；且阿里云普通 ECS 的嵌套虚拟化参数标注「正在邀测中，暂未开放使用」（只有弹性裸金属支持）；云主机无 GPU，过不了 OpenGL 4.1 硬性要求。**双重阻断，别为它花钱。**
2. **USB 真机调试**：云主机无 USB 透传。
3. **无线调试**：官方要求「电脑、设备处于同一网络」。手机在家里路由器 NAT 之后，深圳的云主机**路由不可达**。官方唯一的「远程」方案（服务器侧 `hdc -s IP:8710 -m`）**仍然要求手机旁边有一台 USB 连着手机的机器**。
4. （推断，非官方明文）**UI 预览器**：云主机无硬件 GPU 加速，大概率不可用或极卡。

**另外**：DevEco Studio 官方系统要求只写 **Windows 10 / 11**，**没有列 Windows Server**——而阿里云 Windows 镜像正是 Server 2019/2022/2025。官方从未明文禁止，但也**没有任何官方背书**，第三方仅有一条提及「Server 2022 可能无法识别 Hyper-V」（那说的是模拟器，反正也用不了）。**能否在 Server 上装 DevEco Studio，属于未验证项**。

> **结论**：深圳那台 Windows 的价值，集中在「想要 GUI / 想走 uni-app x 的 HBuilderX」时。**而它的核心能力（命令行构建 + 签名）新加坡这台 Linux 用 CLT 一样能做，且我本来就工作在这里。** 所以建议：**主力走 Linux，Windows 按需启用，不必现在配置。**

---

## 八、里程碑（一期：安卓）

| 阶段 | 内容 | 产出 | 估时 |
|---|---|---|---|
| **M0** | 装 JDK + Android SDK，出一个 Hello World 装到安卓真机 | 手机上有一个能跑的空壳 | 1–2 天 |
| **M1** | 骨架：登录 + 总览 + 通知 + 设置 | 能登录看到自己的分身 | 1 周 |
| **M2** | 对谈：chat + room + socket 实时 | 能在手机上跟分身对话 | 1 周 |
| **M3** | 任务与决策：tasks + decisions | 闭环 | 1 周 |
| **M4** | 打包分发 + 装机图文指引 | 二维码 + 一页纸说明 | 2–3 天 |

**合计约 3–4 周**（双端方案是 4–5 周，省下的是鸿蒙那份 UI 与全部资质工作）。

> **M0 现在简单得多**：安卓没有鸿蒙那些未知——无设备注册、无证书、无 CLT 兼容性问题、无提工单。M0 只是"把工具链装上、确认能出包、能装进手机"，1–2 天足够。

### M0 的先决条件（需要您出手）

1. **一台安卓设备**（手机或平板）用于装机验证 ← **硬前提：没有它，做出来的东西没人能跑**
2. **批准安装**：JDK 17（apt）+ Android SDK（约 10–15G）
3. **磁盘**：本机只剩 30G，安卓工具链占掉一半 → 见拍板项 2

### 待核实项（影响技术选型，不阻塞 M0）

- 卓易通能否让**您自己那台鸿蒙手机**跑安卓 App（决定"只做安卓"对您本人有没有用）
- 小程序容器（FinClip 等）能否直接复用现有 2.65 万行小程序代码
- Tauri 2 在 Linux 上出安卓包的可行性（决定能否复用桌面端 React 代码）

---

## 九、成本

| 项 | 费用 |
|---|---|
| 华为开发者账号（个人实名） | 0 |
| 服务器 / 域名 | 已有（境外，无需备案） |
| 应用商店 | 不涉及 |
| 分发平台（可选，如蒲公英） | 0（免费档够用） |
| 短信验证码（若二期要做） | 阿里云号码认证 1000 次 54 元 |
| **一期合计** | **0 元**（除时间） |

---

## 十、风险清单

| # | 风险 | 级别 | 应对 |
|---|---|---|---|
| 1 | **AppTest 对个人开发者是否开放未明确**（受限开放，需工单） | **高** | **第一天提工单**；不通则退 USB 装机，并把"扫码即装"降级为梦想 |
| 2 | **若手上没有能插手机的电脑，装机就没有退路**（云主机够不着手机） | **高** | 见拍板项 1 |
| 3 | 无 IDE / 无预览器 / 无模拟器，UI 迭代只能"改一次装一次" | 中 | 首页面做简单克制；善用 `devecocli ui screenshot`；本地有电脑则可接 USB 快迭代 |
| 4 | 本机**磁盘只剩 30G**（鸿蒙 SDK + 安卓 SDK 合计可能 20G+） | 中高 | 拍板项 2 |
| 5 | socket.io 客户端在 ArkTS 的可用性未验证 | 中 | 兜底退化为 HTTP 轮询（后端已有 sync 接口） |
| 6 | 调试证书 1 年有效 + 朋友手机需常开开发者模式 | 中 | 定期重签；AppTest 若开通则无此问题 |
| 7 | **DevEco Studio 官方不支持 Windows Server**（若启用深圳那台） | 中 | 先用 CLT 命令行（官方支持）；GUI 属未验证项，按需试 |
| 8 | 纯净模式「增强防护」拦截安装 | 低-中 | 安装时关闭该开关 |
| 9 | `devecocli auth login` 在无图形服务器上的行为未证实 | 低 | 备选：手工 keytool + AGC 网页 + hap-sign-tool |
| 10 | 微信内分发链接被风控 | 低 | 只发二维码 + 系统浏览器 |
| 11 | Push Kit 自分类权益未上架受限（单设备 2 条/日）；个人开发者能否开通说法冲突 | 低 | 一期不做推送；二期实测 AGC 后台 |
| 12 | 「境内个人 + 境外服务器 + 自行分发」的备案义务是灰区 | 低 | 自用不分发；若将来公开，先补备案 |

---

## 十一、需要您拍板的事

1. **测试机：一期做出来装在哪儿？** ← **这一条现在最关键**
   - 您手上那台是纯血鸿蒙，装不了 APK。所以需要**一台安卓设备**（旧手机、平板、朋友的手机都行）做首次装机验证。
   - 没有它，App 做出来没人能跑，也没法验证。
   - 另一个可能的答案：**卓易通**那条路若成立，您自己的鸿蒙手机就能当测试机（正在核实）。

2. **磁盘 30G 怎么办？** 安卓工具链约占 10–15G，剩余空间勉强够但不宽裕。
   - (a) 清理本机；(b) 用深圳那台 Windows 装安卓工具链（80G 够用）；(c) 加块云盘。

3. **一期范围就按 §4 那 6 条？** 有没有必须进一期的（比如购物线、记忆）？

4. **是否现在开始 M0？** 需要您批准安装 JDK 17 + Android SDK（约 10–15G）。

> **已因范围收缩而作废的问题**：华为开发者账号与实名、AppTest 工单、鸿蒙真机、深圳那台 Windows 的定位、先做哪端——**全部不需要了**。鸿蒙相关的先决条件整体推迟到二期。

---

## 附：主要依据来源

- 官方《搭建流水线》（Linux 为例，构建/签名/安装全流程）：https://developer.huawei.com/consumer/cn/doc/harmonyos-guides-V14/ide-command-line-building-app-V14
- 官方《获取 Command Line Tools》（含 macOS/Linux 环境配置）：https://developer.huawei.com/consumer/cn/doc/doccenter-deveco-studio/ide-commandline-get
- 官方 `@deveco/deveco-cli` README（Linux 必须搭配 CLT ≥ 26.0.0）：https://www.npmjs.com/package/@deveco/deveco-cli
- 官方 hdc 文档（支持 Windows/Linux/macOS）：https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/hdc
- 官方论坛答复「DevEco Studio 没有推出 Linux 版本的计划」（2026-01-27）：https://developer.huawei.com/consumer/cn/forum/topic/0203204398025195158
- 官方 Push Kit FAQ（开通无特殊限制）：https://developer.huawei.com/consumer/de/doc/HMSCore-Guides/faq-0000001050042185
- 工信部《关于开展移动互联网应用程序备案工作的通知》（信管〔2023〕105 号）：https://www.gov.cn/zhengce/zhengceku/202308/content_6897341.htm
- uni-app x 鸿蒙开发指南（须本地装 DevEco、无云打包）：https://doc.dcloud.net.cn/uni-app-x/app-harmony/
- 厂商推送上架要求（腾讯云 IM 官方文档）：https://cloud.tencent.com.cn/document/product/269/130368

云主机 / 装机路径专项调研：

- 官方《安装测试包到测试机》（**四条路径穷举，只有 AppTest 免电脑**）：https://developer.huawei.com/consumer/cn/doc/doccenter-tools-faq/faqs-app-debugging-70
- 官方 AppTest 邀请测试 FAQ（**「申请邀请测试，应用需要先备案吗？不需要。」**）：https://developer.huawei.com/consumer/cn/doc/app/agc-help-invite-test-faq-0000002363660365
- 官方 AppTest 用户指南（公开链接 → 系统浏览器 → 安装流程）：https://developer.huawei.com/consumer/cn/doc/app/agc-help-invite-testuser-0000002258071224
- 官方模拟器运行要求（**「不支持在虚拟机系统中运行模拟器」**、OpenGL 4.1）：https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/ide-emulator-requirements
- 官方《使用本地真机运行应用》（**无线调试要求同一 WLAN**）：https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/ide-run-device
- 官方自动签名（**26.0.0 起支持在 AGC 注册设备后签名**）：https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/ide-signing-auto
- 官方 DevEco Studio 系统要求（**仅列 Windows 10/11**）：https://developer.huawei.com/consumer/cn/doc/doccenter-deveco-studio/ide-software-install
- 官方命令行工具 FAQ（Windows PATH 配置、路径过长坑）：https://developer.huawei.com/consumer/cn/doc/doccenter-tools-faq/faqs-app-debugging-70
- 阿里云 Windows 公共镜像（Server 2019 / 2022 / 2025）：https://help.aliyun.com/zh/ecs/user-guide/windows-server
- 阿里云嵌套虚拟化（弹性裸金属支持；普通 ECS 参数标注「邀测中，暂未开放」）：https://help.aliyun.com/zh/ecs/user-guide/elastic-bare-metal-server-overview
- Microsoft RDP 带宽实测（网页浏览 6–6.5 Mbps 档）：https://learn.microsoft.com/en-us/azure/virtual-desktop/rdp-bandwidth
